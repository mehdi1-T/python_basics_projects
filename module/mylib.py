
# Inside it, write three functions:

# One that adds two numbers.
def add(x, y):
    return x + y
# One that multiplies two numbers.
def mutiplie(x, y):
    return x * y
# One that reverses a text string.
def reverse(x):
    return str(x)
