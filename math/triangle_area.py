import math

base =  float(input("enter the base side: "))
hight = float(input("enter the height side: "))

area = math.sqrt(pow(base, 2) + pow(hight, 2))
print(f"the area of the triange is  {round(area  )}cm²")